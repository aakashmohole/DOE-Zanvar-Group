services:
  - type: web
    name: doeproject
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn doeproject.wsgi:application --bind 0.0.0.0:10000 --workers 4 --timeout 120"
    port: 10000  # Explicit port declaration
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "doe-hsag.onrender.com,localhost,127.0.0.1"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://doe-hsag.onrender.com,http://localhost:8000,http://127.0.0.1:8000"
      - key: PORT  # Add this environment variable
        value: "10000"